# Dependensi runtime AI worker.
# SDK provider AI ditambahkan pada Phase 4, ketika classifier dan extractor yang
# memakainya benar-benar dibangun (plan.md §37 Phase 4).
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# Menerima berkas dokumen lewat multipart (plan.md §13.1 UPLOAD).
python-multipart>=0.0.20

# Parser deterministik Phase 3 (plan.md §13.1 PARSER ROUTER, §13.2).
pypdf>=6.0
openpyxl>=3.1
Pillow>=11.0
