# Dependensi runtime AI worker.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# Menerima berkas dokumen lewat multipart (plan.md §13.1 UPLOAD).
python-multipart>=0.0.20

# Parser deterministik Phase 3 (plan.md §13.1 PARSER ROUTER, §13.2).
pypdf>=6.0
openpyxl>=3.1
Pillow>=11.0

# Klien HTTP adapter provider AI (plan.md §13.3). Tidak ada SDK vendor yang dipasang:
# provider dipanggil lewat HTTP biasa supaya menambah vendor baru tidak menambah
# dependensi dan tidak mengunci aplikasi ke satu SDK (plan.md §44.12).
httpx>=0.27
